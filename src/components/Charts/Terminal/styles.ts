import styled from 'styled-components';
import { Box } from '../../Box';

export const ChartWrapper = styled(Box)`
    max-width: 800px;
    height: 400px;
    border-radius: 5px;
    overflow: auto;
`;
