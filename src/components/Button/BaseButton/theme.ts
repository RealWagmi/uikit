import { BaseButtonTheme } from "./types";
import { colors } from "../../../theme/colors";

export const dark: BaseButtonTheme = {
  color: colors.white,
};

export const light: BaseButtonTheme = {
  color: colors.dimGray,
};
