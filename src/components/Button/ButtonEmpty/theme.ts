import { ButtonEmptyTheme } from "./types";
import { colors } from "../../../theme/colors";

export const dark: ButtonEmptyTheme = {
  color: colors.lightFrenchBeige
};

export const light: ButtonEmptyTheme = {
  color: colors.dimGray
};