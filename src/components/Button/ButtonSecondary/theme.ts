import { ButtonSecondaryTheme } from "./types";
import { colors } from "../../../theme/colors";

export const dark: ButtonSecondaryTheme = {
  color: colors.lightFrenchBeige,
  primaryColor: colors.lapisLazuli,
  secondaryColor: colors.frenchSkyBlue
};

export const light: ButtonSecondaryTheme = {
  color: colors.dimGray,
  primaryColor: colors.piggyPink,
  secondaryColor: colors.carnationPink
};
