import { ButtonRadioCheckedTheme } from "./types";
import { colors } from "../../../theme/colors";

export const dark: ButtonRadioCheckedTheme = {
  color: colors.lightFrenchBeige,
  checkColor: colors.white
};

export const light: ButtonRadioCheckedTheme = {
  color: colors.dimGray,
  checkColor: colors.white
};
