
export type TableTheme = {
  background: string,
  activeBackground: string,
  border: string,
  titleColor: string,
  color: string,
  activeColor: string,
  secondColor: string,
  scrollbarTrack: string,
  scrollbarThumb: string,
};